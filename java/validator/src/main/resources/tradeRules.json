{
  "DifferentTCAOpenPriceWithinAQRIDTradeDateRule": {
    "rule": [
      "residualTrades = alltrades.trades.unique().groupBy({trade -> trade.aqrId}).values().findAll({l -> l.size() >= 2});",
      "if (residualTrades.size() > 0) {",
      "return \"Test failed! The Trades with AqrIds in \" + residualTrades.collect({it[0].aqrId}) + \" do not match the business rule!\"",
      "}",
      "return \"Test passed!\""
    ],
    "inputParam": "method receives a Trade POJO and iterates over all trades and di a distinct bla",
    "test": "BusinessTest:differentTCAOpenPriceWithinAQRIDTradeDateRule",
    "doc": "check section 2.3"
  }
}